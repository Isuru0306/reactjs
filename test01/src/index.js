import ReactDOM from "react-dom/client";
import { App } from "./App";

const rootDev = document.getElementById("root");

const reactRoot = ReactDOM.createRoot(rootDev);

reactRoot.render(<App></App>);
